#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char e[16];
int m;
int size;
void gen(int pos,char *result)
{
	int check[10] = {0,};

	for(int i=0;i<size;i++)
	{
		if(check[i]	
	}
}

int main()
{
	int c;
	scanf("%d",&c);
	while(c--)
	{
		scanf("%s %d",e,&m);
		size = strlen(e);
		for(int i=0;i<strlen(e);i++)
		{

		}

	}
}
