#include <stdio.h>
#include <stdlib.h>
#include <string.h>
char *array;
int n;

int func(char *array)
{
	
}

int main()
{
	int c;
	scanf("%d",&c)
	for(int cnt=0;cnt<c;cnt++)
	{
		scanf("%d",&n);
		array =(char*)malloc(sizeof(char)*n);
		scanf("%s",array);
	}

}
